apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aws-route53-zone-exporter.fullname" . }}-test-connection"
  labels:
    {{- include "aws-route53-zone-exporter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aws-route53-zone-exporter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
